selenium -> program to automatically execute some tasks.
junit -> created in eclipse, a file to be tested.
jmeter -> can execute some junit tests and measure the performance (exhaust)


-> to you use the selinium, you need to install the google plugin first, open it, and then execute the code.
-> you need also to add some jars. to do that you go the referenced libraries, right click, build path, and then add. ( you can not just copy paste)
